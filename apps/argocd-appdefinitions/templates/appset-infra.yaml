apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-infra
  namespace: {{ .Values.argocdNamespace }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "1"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - list:
      elements: 
      {{ .Values.repos.infra | toYaml | nindent 6 | trim }}
  template:
    metadata:
      name: "{{`{{.chartName}}`}}"
      namespace: {{ .Values.argocdNamespace }}
      annotations:
        argocd.argoproj.io/sync-wave: "{{`{{ .syncWave | quote }}`}}"
    spec:
      project: "default"
      source:
        repoURL: "{{`{{.repoURL}}`}}"
        targetRevision: "{{`{{.revision}}`}}"
        path: "{{`{{.path}}`}}"
        helm:
          skipCrds: true # We don't want these managed by helm so we can self-install and avoid annoying race conditions
      destination:
        server: "{{`{{.server}}`}}"
        namespace: "{{`{{ .namespace | default .chartName}}`}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
